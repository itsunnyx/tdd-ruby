import { test } from '@playwright/test';

// Outside-in TDD: write E2E tests here first, then build the Rails app.
// Domain logic from 01 (Cart, Checkout, rules) can be ported into app/.

test.skip('add item to cart', async ({ page }) => {
  // TODO: first failing test goes here
});
